{erl_opts, [debug_info, {parse_transform, lager_transform}]}.
{deps, [{lager, "3.5.2"}]}.
{shell, [{apps, [lager, spinet]}]}.

{profiles, [
    {test, [
        {deps, [{mockgyver, "0.12.0"}]},
        {erl_opts, [debug_info]}
    ]}
]}.